{
  "name": "typescript-bar-chart",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc && cp src/index.html dist/index.html && cp -r src/css dist/",
    "start": "nodemon server.js"
  },
  "devDependencies": {
    "express": "^4.19.2",
    "http-proxy-middleware": "^3.0.0",
    "live-server": "^1.2.1",
    "typescript": "^4.0.0",
    "nodemon": "^3.1.3"
  }
}
